import React from 'react';

const Feedback = () => {
    return (
        <div  className="relative bg-cover bg-no-repeat bg-center mb-9"
        style={{
          backgroundImage: `url(${ban.image})`,
          height: "90vh",
        }}>
            
        </div>
    );
};

export default Feedback;